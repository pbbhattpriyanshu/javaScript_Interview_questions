//Event Capturing
// A propagation model where events are handled from outer elements to inner elements

//Event Bubbling
// A propagation model where events are handled from inner elements to outer elements